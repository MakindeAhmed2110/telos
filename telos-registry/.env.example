PORT=4010

# Log list duration to stdout: GET /v1/agents (see also Server-Timing in browser DevTools → Network).
# TELOS_REGISTRY_LOG_TIMING=1

# --- File mode (default) ---
# Leave contract vars empty; data lives in data/agents.json

# --- On-chain mode ---
TELOS_REGISTRY_CONTRACT_ID=CDDSHGZ3AFQT3PJY2XFBOG7STKEBDBG2XORM6TROMMMOZLMM57PZJGLE
TELOS_REGISTRY_SOURCE_ACCOUNT=alice
TELOS_REGISTRY_NETWORK=testnet

# Production / CI: paste signer secret (never commit). Imports into Stellar CLI at startup.
# TELOS_REGISTRY_SIGNER_SECRET=SXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# If import fails on Windows, run once: echo 'S...' | stellar keys add alice --overwrite
# then set TELOS_REGISTRY_SKIP_STELLAR_BOOTSTRAP=1
# TELOS_REGISTRY_SKIP_STELLAR_BOOTSTRAP=1
