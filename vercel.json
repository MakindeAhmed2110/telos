{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "corepack enable pnpm && pnpm -C frontend install --frozen-lockfile",
  "buildCommand": "corepack enable pnpm && pnpm -C frontend run build",
  "outputDirectory": "frontend/build/client",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
